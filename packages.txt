List of Python libraries required to run the code:

streamlit
python-docx
pandas
seaborn
numpy
scikit-learn
nltk (Natural Language Processing Toolkit)
plotly

# Safe defaults if no topics
                if st.session_state.topics_data and st.session_state.topic_prevalence is not None:
                    dominant_topics = ', '.join([f"Topic {i+1}" for i in np.argsort(st.session_state.topic_prevalence)[-3:][::-1]])
                else:
                    dominant_topics = "Not available (run topic modeling first)"